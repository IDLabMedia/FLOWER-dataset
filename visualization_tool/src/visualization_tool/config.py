from pathlib import Path

DATABASE_PATH = Path(__file__).parent.joinpath("metadata.db")

# Path where dataset can be found in same folder structure as downloaded.
DATA_PATH = Path("../FLOWER Dataset/")
